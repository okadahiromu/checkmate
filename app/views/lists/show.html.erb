<h1><%= @list.name %></h1>

<p><%= @list.description %></p>

<h2>アイテム一覧</h2>
<ul>
  <% @list.items.each do |item| %>
    <li>
      <%= item.name %>
      <%= item.description %>
    </li>
  <% end %>
</ul>

<%= link_to 'リストを編集', edit_list_path(@list) %>