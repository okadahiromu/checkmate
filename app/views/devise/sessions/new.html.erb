<!DOCTYPE html>
<html>
<head>
  <title>Sign in to CheckMate</title>
  <%= stylesheet_link_tag "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <header>
    <img src="https://example.com/your-image.jpg" alt="CheckMate Logo">
    <h1><span class="app-name">CheckMate</span> Sign in</h1>
    <p>Log in to your account</p>
  </header>

  <div class="container">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="mb-3">
        <%= f.label :email, class: "form-label" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", placeholder: "Your Email" %>
      </div>

      <div class="mb-3">
        <%= f.label :password, class: "form-label" %>
        <%= f.password_field :password, autocomplete: "current-password", class: "form-control", placeholder: "Your Password" %>
      </div>

      <div class="mb-3">
        <%= f.submit "Sign in", class: "btn btn-primary btn-lg btn-block" %>
      </div>
    <% end %>

    <p class="text-center">Don't have an account? <%= link_to "Sign up", new_user_registration_path %></p>
  </div>
</body>
</html>
